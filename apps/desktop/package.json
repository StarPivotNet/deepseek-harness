{
  "name": "@deepseek-ai/dsh-desktop",
  "description": "Electron window around the local dsh web Host",
  "version": "0.1.3-alpha.2.1",
  "private": true,
  "type": "module",
  "main": "lib/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "pnpm run build && electron .",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "license": "MIT",
  "devDependencies": {
    "electron": "^39.0.0",
    "typescript": "^6.0.3"
  }
}
